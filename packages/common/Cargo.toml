[package]
name = "common"
version = "0.1.0"
edition = "2021"

[lib]
crate-type = ["rlib"]

[dependencies]
soroban-sdk = { workspace = true }

[dev-dependencies]
proptest = { version = "1.4.0", default-features = false, features = ["alloc"] }
